all:
  vars:
    sops: "{{ lookup('community.sops.sops', 'secrets.yaml') | ansible.builtin.from_yaml }}"
    ansible_user: "{{ sops.ansible_user }}"
  hosts:
    bn-kube-01.int.bollingnet.net:
    bn-kube-02.int.bollingnet.net:
    bn-kube-03.int.bollingnet.net:
    bn-kube-04.int.bollingnet.net:
    bn-kube-05.int.bollingnet.net:
  children:
    docker:
      hosts:
        bn-docker-01.int.bollingnet.net:
        bn-docker-02.int.bollingnet.net:
